# ── OS cruft ──────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini

# ── editors / IDEs ────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.*.sw[a-p]

# ── Claude Code (keep shared settings.json, ignore personal/local) ─────────────
.claude/settings.local.json

# ── logs / temp ───────────────────────────────────────────────────────────────
*.log
tmp/
.cache/

# ── local scratch: projects you scaffold while testing recipes ─────────────────
# (uncomment / adjust if you generate test projects inside the repo)
# /sandbox/
# /.test-projects/
