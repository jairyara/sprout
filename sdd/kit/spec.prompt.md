# Spec brief — fill this in, then run the SDD flow

> This is the **editable starting point** for a new feature. Replace the prompts below
> with your own words (bullet fragments are fine — the agent will turn them into a real
> spec). When it's ready, ask the agent to **"draft the spec"** and it will invoke the
> `sdd-spec` skill, read this file, ask what's missing, and write
> `sdd/specs/<feature>/spec.md`. From there: plan → build → verify.

## Feature name

<!-- short kebab-case id, e.g. user-invite-flow -->

## What I want (the goal)

<!-- 1–3 sentences. The outcome, not the implementation. -->

## Who it's for

<!-- the user / actor and the job they're trying to get done -->

## Why now / the problem

<!-- what's broken or missing today -->

## Acceptance criteria

<!-- bullet list of observable, checkable outcomes. "Done" = all of these are true. -->
- 
- 

## Out of scope (non-goals)

<!-- things to explicitly NOT do, so the plan doesn't sprawl -->
- 

## Constraints & context

<!-- existing code to reuse, APIs, data shapes, perf/security limits, deadlines -->
- 

## Open questions

<!-- anything you're unsure about — the agent will help resolve these in sdd-spec -->
- 

## Skills to use / add

<!-- The capabilities available to this project are listed in AGENTS.md: the "Auto-invoke"
table is already installed; "More skills you can add" lists installable ones from the
catalog (or run `sprout list`). If this feature would benefit from a skill that isn't
installed yet, NAME IT here — when you draft the spec the agent will surface the relevant
ones and offer to add them (`sprout skills add <name>`) before building. You don't need to
know exact names; describing the need is enough ("something for Postgres schemas"). -->
- 
