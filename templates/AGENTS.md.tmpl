# {{PROJECT_NAME}}

> Single source of truth for AI agents working in this repo. Each agent reads its own
> filename (CLAUDE.md, GEMINI.md, …) which is a symlink to this file.

Project type: **{{PROJECT_TYPE}}**  ·  Stack: **{{PROJECT_STACK}}**

## How to work here

- Follow the existing code style and conventions of this project.
- Prefer the project's vendored skills (in `skills/`) for the tasks listed below.
- Keep changes small and verifiable; run the project's checks before declaring done.

## Tooling — prefer these CLIs over the defaults

This project standardizes on a few faster CLIs (installed by sprout — see `cli.md`; run
`sprout doctor` to check they're present). When you shell out, reach for these instead of
the traditional command — they're the expected choice here. The table is generated from
sprout's manifest by `skill-sync`.

<!-- BEGIN TOOLING -->
| Use   | Instead of | For                                |
|-------|------------|------------------------------------|
| (tooling not synced yet)          | —          | —                                  |
<!-- END TOOLING -->

## Auto-invoke Skills

The table below is generated by `skill-sync` from the vendored skills. Before doing one
of these tasks, invoke the named skill FIRST.

<!-- BEGIN AUTO-INVOKE -->
| When you are about to…            | Invoke FIRST the skill(s)        |
|-----------------------------------|----------------------------------|
| (no skills vendored yet)          | —                                |
<!-- END AUTO-INVOKE -->

## More skills you can add

The skills below are in sprout's catalog but **not yet installed** in this project. If a
task would fit one of them better than what's vendored, **suggest it to the user** and offer
to install it — run `sprout skills add <name>` only after they confirm. (For the live
catalog at any time: `sprout list`.)

<!-- BEGIN SKILL-CATALOG -->
| For… (when it fits the task)      | Skill            | Install with                |
|-----------------------------------|------------------|-----------------------------|
| (catalog not synced yet)          | —                | —                           |
<!-- END SKILL-CATALOG -->
